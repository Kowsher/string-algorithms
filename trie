///trie is a string search algorithm
#include<bits/stdc++.h>
using namespace std;
#define MAX_NODE 1000000
#define MAX_LEN 1000
int node[MAX_NODE][130];
/* assuming only small letters 'a'-'z '*/
int root,nnode;
int isWord[MAX_NODE];
/* intialize before making a trie */
void initialization(){
    root = 0;
    nnode = 0;
    for(int i=0;i<130;i++) node[root][i] = -1;
}
void insert(string S){
    int len = S.size();
    int now = root;
    for(int i=0;i<len;i++){
        if(node[now][S[i]]==-1){
            node[now][S[i]] = ++nnode;
        }
        for(int j=0;j<130;j++) node[nnode][j] = -1;
        now = node[now][S[i]];
    }
    isWord[now]=1;
}
bool search(string S){
    int len = S.size();
    int now = root; int chk=0;
    for(int i=0;i<len;i++){
        if(node[now][S[i]]==-1){
            chk=1;
        }
        now = node[now][S[i]];
    }
    if(chk==0 && isWord[now]==1) return 1;
    return 0;
}
int main()
{
    int n; string str;
    cin>>n;
    initialization();
    while(n--){
    cin>>str;
    insert(str);
    }
    int q; cin>>q;
    while(q--){
    cin>>str;
    if(search(str)==1) cout<<"Found\n";
    else cout<<"Not found\n";
    }
    return 0;
}
